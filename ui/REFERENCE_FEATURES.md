# Новые возможности справочников

## 🔍 Поиск и фильтрация

### Быстрый поиск
Введите текст в поле **"Поиск"** — таблица автоматически отфильтруется:
- Поиск по **всем колонкам** одновременно
- **Без учёта регистра**
- Скрытие неподходящих строк (не удаление)
- Счётчик показывает: **"Записей: 5 из 20"** (найдено / всего)

### Показать удалённые
Чекбокс **"Показать удалённые"**:
- ☐ — показывать только активные записи (по умолчанию)
- ☑ — показывать все записи, включая помеченные как удалённые

---

## ⌨️ Горячие клавиши

| Клавиша | Действие |
|---------|----------|
| **Insert** | Добавить новую запись |
| **Enter** | Редактировать выбранную запись |
| **Delete** | Удалить выбранную запись |
| **F5** | Обновить данные из базы |
| **Esc** | Закрыть окно справочника |

---

## 🖱️ Управление мышью

### Двойной клик
Двойной клик по строке → открывает форму редактирования (аналогично Enter).

### Контекстное меню (ПКМ)
Правый клик по таблице → меню:
- **Добавить** — создать новую запись
- **Изменить** — редактировать выбранную запись (если строка выбрана)
- **Удалить** — удалить выбранную запись (если строка выбрана)
- **Обновить** — перезагрузить данные из БД

---

## 📊 Визуальные улучшения

### Чередующиеся цвета строк
Строки таблицы окрашены в чередующиеся цвета (белый / светло-серый) для лучшей читаемости.

### Счётчик записей
В нижней части окна отображается:
- **"Записей: 20"** — при отсутствии фильтрации
- **"Записей: 5 из 20"** — при активном поиске (показано / всего)

### Подсказки (tooltips)
Наведите курсор на кнопку → появится подсказка с действием и горячей клавишей:
- *"Добавить новую запись (Insert)"*
- *"Изменить выбранную запись (Enter или двойной клик)"*
- *"Удалить выбранную запись (Delete)"*
- *"Обновить данные из базы (F5)"*
- *"Закрыть окно (Esc)"*

### Кнопка очистки поиска
Поле поиска имеет кнопку **"✕"** для быстрой очистки фильтра.

### Сортировка колонок
Кликните по заголовку колонки → таблица отсортируется по этой колонке (↑/↓).

---

## 🎯 Примеры использования

### Быстрый поиск категории
1. Откройте **Справочники → Категории товаров**
2. Начните вводить в поле поиска: `"напи"` → найдутся "Напитки"
3. Таблица покажет только подходящие записи

### Просмотр удалённых записей
1. Откройте любой справочник
2. Установите чекбокс **"Показать удалённые"** → появятся все записи
3. Удалённые записи будут помечены "Да" в колонке "Удалён"

### Быстрое добавление
1. Нажмите **Insert** (или кнопку "Добавить")
2. Заполните данные в диалоге
3. Новая запись появится в таблице

### Редактирование двойным кликом
1. Дважды кликните по любой строке
2. Откроется форма редактирования
3. Измените данные и сохраните

### Удаление через контекстное меню
1. Кликните **правой кнопкой** по записи
2. Выберите **"Удалить"** в меню
3. Подтвердите действие

---

## 💡 Советы

- **Комбинируйте поиск и фильтр удалённых**: например, найдите все удалённые категории, содержащие слово "тест"
- **Используйте F5 для обновления**: если данные изменились в другом окне или БД
- **Сортировка + поиск**: сначала отсортируйте таблицу, потом примените поиск
- **Горячие клавиши**: работают быстрее, чем мышь — запомните их для эффективной работы

---

## ⚙️ Технические детали

Все эти возможности реализованы в базовом классе **`ReferenceDialog`** и **автоматически доступны** во всех справочниках:
- Категории товаров ✅
- Товары ✅
- Услуги (будущее)
- Ставки НДС (будущее)
- Любые новые справочники

При создании нового справочника все эти функции работают **из коробки** — не нужно ничего дополнительно программировать!
